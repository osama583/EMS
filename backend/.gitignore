.env
# Any .env variant - backups and per-environment copies hold the same secrets.
.env.*
!.env.example
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.log
# Uploaded images are user data, not source - they land here at runtime.
var/uploads/
